# GV Baseline Comparison

This compares GV recoverability detection against simple baseline detectors:

- rolling z-score anomaly detection
- rolling variance anomaly detection

Purpose:

Do not claim GV is useful unless it beats simpler methods.

Output:

`reports/gv_baseline_comparison/baseline_comparison.csv`
