# GV Kernel Runtime

Purpose:

Unify GV into one recoverability-aware intelligence kernel.

Core principle:

GV does not judge intelligence by correctness alone.

GV evaluates whether intelligence remains recoverable over time.

Kernel path:

input
→ score
→ trajectory memory
→ intervention
→ arbitration
→ runtime telemetry
→ output
